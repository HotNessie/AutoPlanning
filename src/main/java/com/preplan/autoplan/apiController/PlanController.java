package com.preplan.autoplan.apiController;

import com.preplan.autoplan.dto.PlanDto;
import com.preplan.autoplan.dto.plan.PlanCreateDto;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/plans")
@RequiredArgsConstructor
public class PlanController {
//    private final PlanService planService;

//    @PostMapping
//    public ResponseEntity<PlanDto> createPlan(@RequestBody @Valid PlanCreateDto planCreateDto) {
//         계획 생성 로직
//    }

//    @GetMapping("/{id}")
//    public ResponseEntity<PlanDto> getPlan(@PathVariable Long id) {
    // 계획 조회 로직
//    }

    // 기타 계획 관련 엔드포인트
}